# Example production env for Vercel builds.
# Copy to `.env.production` in CI or set the same env var in Vercel Project Settings.
# Replace <YOUR_RENDER_BACKEND> with the Render service URL (for example: https://nexus-backend.onrender.com)
VITE_BACKEND_URL="https://<YOUR_RENDER_BACKEND>"
